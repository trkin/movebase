<%
  breadcrumb Club.model_name.human(count: 2) => nil
%>

<%= button_tag_open_modal new_admin_club_path do %>
  <i class="demo-icon icon-plus" aria-hidden="true"></i>
  <%= t_crud('add_new', Club) %>
<% end %>
<br>
<%= @datatable.render_html search_admin_clubs_path(format: :json) %>
