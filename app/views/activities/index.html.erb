<%
  breadcrumb Activity.model_name.human(count: 2) => nil
%>

<%= button_tag_open_modal new_activity_path do %>
  <i class="demo-icon icon-plus" aria-hidden="true"></i>
  <%= t_crud('add_new', Activity) %>
<% end %>

<%= @datatable.render_html search_activities_path(format: :json) %>
